<?php
namespace Modular\Blocks;
/**
 * SubBlocks block has another layer of indirection so has it's own associated blocks which can be managed separately
 * to the Block itself, so in config it also has 'HasBlocks' extension.
 */

class SubBlocks extends Block {

}